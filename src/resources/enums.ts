export enum AuthorizationRequest {
    Pending = 1,
    Accepted = 2,
    Rejected = 3
}

export enum AssetTransactionType {
    IssueToken = 1
}

export enum AssetTransactionStatus {
    Pending = 1,
    Accepted = 2,
    Rejected = 3
}